@group('section_duo_text')
    <section class="duo-text container flex flex-row justify-between items-start" id="readMore">
        <div class="wrap-50">
            @sub('text_left')
        </div>
        <div class="wrap-50">
            @sub('text_right')
        </div>
    </section>
@endgroup